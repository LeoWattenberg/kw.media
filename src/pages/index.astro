---
import StructureReview from '../components/StructureReview.astro';
import BaseLayout from '../components/layout/BaseLayout.astro';
import { findPagePlan } from '../data/pages';

const page = findPagePlan('/');
---

<BaseLayout locale="de" title={page?.title ?? 'Astro Migration'}>
	<StructureReview locale="de" currentPage={page} />
</BaseLayout>
