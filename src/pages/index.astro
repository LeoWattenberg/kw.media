---
import { findSitePage, getSitePageAlternatePaths, getSitePageComponent } from '../data/pages';

const page = findSitePage('/');

if (!page) {
	throw new Error('Missing homepage content');
}

const PageComponent = getSitePageComponent(page);
---

<PageComponent page={page} alternatePaths={getSitePageAlternatePaths(page)} />
