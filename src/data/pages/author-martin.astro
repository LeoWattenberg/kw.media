---
import SitePageShell from '../../components/SitePageShell.astro';
import { definePageModule } from '../../lib/site-page-module';
import translations from './author-martin.json';

export const pageModule = definePageModule('author-martin', translations);

const { page, alternatePaths } = Astro.props;
---

<SitePageShell page={page} alternatePaths={alternatePaths} />
